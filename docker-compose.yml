version: '3.8'

services:
  whmcs-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whmcs-landing
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
